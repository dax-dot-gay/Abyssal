[server]
address = "0.0.0.0"
port = 5174
secret_key = "Lcp8BBFL1hWxiMAoOQstl+tt9ntBlCdeoMpDlX5IMM8="

[server.tls]
key = "certs/key.pem"
certs = "certs/cert.pem"

[auth]
admin_user = "admin"
admin_password = "admin"

[database]
url = "mongodb://abyssal:abyssal@localhost:27017"
database = "abyssal"

[filesystem]
filesystem = "/mnt/shared"
enforce_ownership = false

[filesystem.directories]
root = {display_name = "Root", path = "/"}
